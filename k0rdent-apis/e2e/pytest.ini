[pytest]
testpaths = tests
markers =
    smoke: short lab smoke against live k0rdent-apis + k8s
    hcp: hosted control-plane cluster scenarios
    bmaas: instance-group / bare-metal scenarios
timeout = 1800
addopts = -ra
