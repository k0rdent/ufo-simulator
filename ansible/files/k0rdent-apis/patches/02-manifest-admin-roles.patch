diff --git a/scripts/post-deploy/envs/kind/manifest.yaml b/scripts/post-deploy/envs/kind/manifest.yaml
--- a/scripts/post-deploy/envs/kind/manifest.yaml
+++ b/scripts/post-deploy/envs/kind/manifest.yaml
@@ -129,6 +129,23 @@ policies:
     principal: {type: user, id: usr-acme-bob}
     scope: {type: project, org: acme, project: acme-main}
     displayName: Bob - compute-viewer (acme-main)
+  # Lab-only platform-scope grants for the kind admin. The stock fixture scopes
+  # compute-admin to the kind-main project and binds no infrastructure/cluster
+  # admin at all; the UFO lab drives switches, softgates and cluster objects
+  # that live at platform scope, so usr-kind-admin needs them there.
+  # iam-admin (org kind) is upstream already — do not re-add it here.
+  - role: infrastructure-admin
+    principal: {type: user, id: usr-kind-admin}
+    scope: {type: platform}
+    displayName: Kind Admin - infrastructure-admin (platform)
+  - role: cluster-admin
+    principal: {type: user, id: usr-kind-admin}
+    scope: {type: platform}
+    displayName: Kind Admin - cluster-admin (platform)
+  - role: compute-admin
+    principal: {type: user, id: usr-kind-admin}
+    scope: {type: platform}
+    displayName: Kind Admin - compute-admin (platform)
 
 identityProviders:
 # In-cluster mock-oauth2-server (k0rdent-kind-extras). Each row's issuer path +
