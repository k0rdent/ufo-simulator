diff --git a/Makefile b/Makefile
--- a/Makefile
+++ b/Makefile
@@ -1218,11 +1218,8 @@
 	@bash scripts/kind/pf-up.sh $(KIND_KUBECONTEXT) $(KIND_NAMESPACE) $(KIND_PF_LOG_DIR) \
 		auth:8083:80 mock-oauth2-server:8190:8080
 	@echo ""
-	@echo "IdP mock ready. NOTE: KCS-1407 made the mock unregisterable (the discoveryUrl"
-	@echo "guard refused plaintext http to a ClusterIP) AND re-seeded kind onto real"
-	@echo "Cognito pools. KCS-1468 lifted the guard half — kind now sets both opt-outs —"
-	@echo "but the seed half stands, so nothing resolves to this mock and this fails:"
-	@echo "  make kind-multi-verify-mock-oauth   # KNOWN BROKEN — see the script header"
+	@echo "IdP mock ready. Verify a full login through it:"
+	@echo "  make kind-multi-verify-mock-oauth   # both callback modes, as admin@kind.test"
 
 kind-ui-up: ## (kind-up) Install the Atlas UI, start its missing port-forwards, and show the /etc/hosts entry browser sign-in needs
 	@$(require-offset-0)
@@ -1236,8 +1233,8 @@
 	@echo "for in-browser sign-in that one name must resolve to localhost. /etc/hosts:"
 	@$(MAKE) --no-print-directory kind-ui-hosts-hint HOST=mock-oauth2-server.$(KIND_NAMESPACE).svc
 	@echo ""
-	@echo "Then open http://localhost:3010 and sign in with a real Cognito user"
-	@echo "(vendor.com / testcustomer.com) — since KCS-1407 admin@kind.test cannot log in."
+	@echo "Then open http://localhost:3010 and sign in as admin@kind.test — the mock"
+	@echo "auto-redirects (interactiveLogin: false), so there is no password to type."
 	@echo "Keycloak is NOT in the browser path — the platform token is minted server-side,"
 	@echo "so k0rdent.dev.local needs no hosts entry. Atlas's own system API calls still"
 	@echo "need ATLAS_API_KEY=<key> (re-run with it set once minted)."
@@ -1887,7 +1884,7 @@
 	@echo "k0rdent-kind-extras (mock IdP) installed on $(KIND_GLOBAL_CLUSTER). Next:"
 	@echo "  make kind-multi-provision-mock     # register the IdP + org/user via the service APIs (no SQL)"
 	@echo "  make kind-multi-port-forward       # re-run to expose the mock at http://localhost:8190"
-	@echo "  make kind-multi-verify-mock-oauth  # KNOWN BROKEN — kind seeds real Cognito, not the mock (see script header)"
+	@echo "  make kind-multi-verify-mock-oauth  # full OIDC login as admin@kind.test, both callback modes"
 	@echo "  make kind-multi-e2e-compute-kong   # compute accessibility test through the regional Kong"
 
 kind-multi-provision-mock: ## Provision the kind real-auth env (org+user+IdP -> mock) on the GLOBAL cluster via the provision tool (no SQL); requires kind-multi-install-extras first
@@ -1902,10 +1899,10 @@
 		echo "k0rdent-kind-extras release not present on $(KIND_GLOBAL_CLUSTER) — nothing to uninstall."; \
 	fi
 
-kind-multi-verify-mock-oauth: ## KNOWN BROKEN (KCS-1407 seeded kind onto real Cognito IdPs, so nothing resolves to the in-cluster mock; KCS-1468 lifted the discoveryUrl guard half, the seed half stands) — see the header of scripts/kind/verify-mock-oauth.sh
+kind-multi-verify-mock-oauth: ## Drive a full OIDC login as admin@kind.test through the in-cluster mock IdP and assert the resulting platform JWT is Keycloak-signed; covers both callback delivery modes (KCS-1363)
 	@bash scripts/kind/verify-mock-oauth.sh
 
-kind-multi-e2e-compute-kong: sdk-client-py-regen ## Run the compute Kong-mode accessibility test through the REGIONAL Kong with a real JWT. FAILS (not skips) if no JWT can be acquired — since KCS-1407 kind seeds real Cognito IdPs, so the mock-IdP login this drives needs rework; E2E_KONG_JWT_OPTIONAL=1 restores the old skip
+kind-multi-e2e-compute-kong: sdk-client-py-regen ## Run the compute Kong-mode accessibility test through the REGIONAL Kong with a real JWT. FAILS (not skips) if no JWT can be acquired; E2E_KONG_JWT_OPTIONAL=1 restores the old skip
 # Both contexts are explicit because this is a MULTI-cluster run and the two
 # kubectl-backed fixtures live on different clusters: compute (its DB, via
 # compute_db.py) is regional, organizations (scaled to 0 by the fail-closed quota
diff --git a/deploy/helm/k0rdent-apis/values-secret-kind-global.yaml b/deploy/helm/k0rdent-apis/values-secret-kind-global.yaml
--- a/deploy/helm/k0rdent-apis/values-secret-kind-global.yaml
+++ b/deploy/helm/k0rdent-apis/values-secret-kind-global.yaml
@@ -58,11 +58,11 @@
   # identity providers are reachable. An on-prem install whose IdP has TLS
   # should set idpAllowPrivateTargets alone and leave the scheme gate off.
   #
-  # These make an in-cluster mock IdP registrable again, but they do NOT on
-  # their own restore `make kind-multi-verify-mock-oauth`. KCS-1407 also
-  # re-seeded scripts/post-deploy/envs/kind/manifest.yaml onto real Cognito
-  # pools, and the mock fixture users do not exist there. Restoring that flow
-  # needs a manifest change too, which belongs to the kind local-env tooling.
+  # These make an in-cluster mock IdP registrable, which is one of the two
+  # halves `make kind-multi-verify-mock-oauth` needs. The other is the seed:
+  # KCS-1407 re-seeded scripts/post-deploy/envs/kind/manifest.yaml onto real
+  # Cognito pools, and KCS-1468 moved it back onto the mock. Both are in place
+  # now, so clearing either gate here breaks that flow.
   idpAllowPrivateTargets: true
   idpAllowInsecureScheme: true
   # KCS-235: secrets under global so subcharts can hash per-service subsets.
diff --git a/deploy/helm/k0rdent-apis/values-secret-kind.yaml b/deploy/helm/k0rdent-apis/values-secret-kind.yaml
--- a/deploy/helm/k0rdent-apis/values-secret-kind.yaml
+++ b/deploy/helm/k0rdent-apis/values-secret-kind.yaml
@@ -39,9 +39,9 @@
   # values-secret-kind-global.yaml. Keeping only the multi-cluster file in sync
   # would leave the two kind topologies silently different.
   #
-  # Inert as things stand: the kind manifest seeds real Cognito on both
-  # topologies, so nothing currently registers the mock. Set here so the two
-  # overlays agree rather than diverging the moment someone re-seeds it.
+  # Load-bearing: the kind manifest seeds the in-cluster mock on both
+  # topologies (KCS-1468), so clearing either gate makes the seed fail to
+  # register.
   #
   # Dev/kind only — a public SaaS deploy leaves both unset.
   idpAllowPrivateTargets: true
diff --git a/deploy/helm/k0rdent-kind-extras/README.md b/deploy/helm/k0rdent-kind-extras/README.md
--- a/deploy/helm/k0rdent-kind-extras/README.md
+++ b/deploy/helm/k0rdent-kind-extras/README.md
@@ -1,6 +1,6 @@
 # k0rdent-kind-extras
 
-> **Superseded for the login path by KCS-1407, half-restored by KCS-1468.**
+> **Was superseded for the login path by KCS-1407; restored under KCS-1468.**
 > KCS-1407 screened a provider's `oidcConfig.discoveryUrl` as an outbound
 > destination — `https` to a public host only, with private/loopback/link-local
 > and cloud-metadata targets refused — which made this mock (plaintext `http` on
@@ -9,14 +9,18 @@
 >
 > KCS-1468 added operator opt-outs and kind sets both
 > (`global.idpAllowPrivateTargets`, `global.idpAllowInsecureScheme`), so the
-> screening no longer blocks this mock: it **can** be registered as an identity
-> provider again.
+> screening no longer blocks this mock, and the kind manifest has since been
+> re-seeded back onto it. Both halves are needed: a registrable mock that
+> nothing resolves to still fails. Everything below is current again.
 >
-> What is still not restored is the seed. The kind manifest points at real
-> Cognito, so nothing resolves to this mock and the fixture users do not exist
-> in those pools; `make kind-multi-verify-mock-oauth` still fails for that
-> reason alone. Re-seeding a mock IdP row belongs to the kind local-env tooling.
-> Everything below describes the pre-KCS-1407 behaviour.
+> One caveat on a cluster seeded differently before. Two limits hold whatever
+> `provision apply` does with a row it recognises: it never deletes a row the
+> manifest has dropped, and it cannot update the org-less CIAM fallback — no
+> internal-listener route exposes that partition for reading or patching. So a
+> cluster previously seeded onto Cognito can keep stale domain claims and a
+> Cognito fallback row, and `bob@acme-eng.test` resolves through exactly that
+> fallback. Seed onto a greenfield cluster instead: `make kind-multi-down &&
+> make kind-multi-up`.
 
 Kind-only test infrastructure that sits **alongside** (not inside) the
 `k0rdent-apis` umbrella chart. It lets the auth-service's full federated login
diff --git a/scripts/kind/provision-mock-idp.sh b/scripts/kind/provision-mock-idp.sh
--- a/scripts/kind/provision-mock-idp.sh
+++ b/scripts/kind/provision-mock-idp.sh
@@ -31,32 +31,26 @@
 
 fail() { echo "ERROR: $*" >&2; exit 2; }
 
-# KCS-1407: the kind manifest no longer points at the in-cluster mock IdP — a
-# customer-supplied discoveryUrl is screened as an outbound destination, and an
-# in-cluster IdP cannot satisfy that by construction — so it seeds the same real
-# Cognito pools mock01/dev01/dev02 use. Their client secrets are real and come
-# from the operator's environment; the previous KIND_IDP_SECRET throwaway is
-# gone because nothing references it any more.
+# KCS-1468: the kind manifest points back at the in-cluster mock IdP, so the one
+# secret every row reads is a throwaway — mock-oauth2-server issues on the
+# client_id match alone and never checks the secret, so any non-empty value does.
+# It is still required to be SET: an IdP row carrying an empty client secret is
+# not the same registration, and kind should fail here rather than register one.
 #
-# Preflight here rather than letting `provision apply` fail: SecretRef.Resolve
-# does a bare os.LookupEnv (platform/go-tools/internal/manifest/manifest.go), so
-# a missing name surfaces mid-apply without saying which one, after the manifest
+# Preflight rather than letting `provision apply` fail: SecretRef.Resolve does a
+# bare os.LookupEnv (platform/go-tools/internal/manifest/manifest.go), so a
+# missing name surfaces mid-apply without saying which one, after the manifest
 # has already written orgs and users.
-missing=""
-for v in VENDOR_IDP_CLIENT_SECRET PROVIDER_IDP_CLIENT_SECRET CIAM_IDP_CLIENT_SECRET; do
-    eval "val=\${$v:-}"
-    [ -n "$val" ] || missing="$missing $v"
-done
-if [ -n "$missing" ]; then
-    fail "missing IdP client secret(s):$missing
-  The kind manifest seeds real Cognito pools (KCS-1407), so these must be exported
-  before provisioning. They are the same names mock01/dev01/dev02 use — see
-  docs/operations/environment-bootstrap.md:
-      export VENDOR_IDP_CLIENT_SECRET=...
-      export PROVIDER_IDP_CLIENT_SECRET=...
-      export CIAM_IDP_CLIENT_SECRET=...
-  (scripts/post-deploy/seed.sh uses IDP_*_SECRET for the same pools; that is the
-  SQL seed path and its names are not interchangeable with these.)"
+#
+# Nothing here reads VENDOR_/PROVIDER_/CIAM_IDP_CLIENT_SECRET any more: those
+# name real Cognito pools, and kind no longer seeds any.
+if [ -z "${KIND_IDP_SECRET:-}" ]; then
+    fail "missing IdP client secret: KIND_IDP_SECRET
+  Every identityProviders row in the kind manifest reads this one name. The
+  in-cluster mock-oauth2-server does not validate it, so any value works:
+      export KIND_IDP_SECRET=placeholder
+  (scripts/post-deploy/seed.sh uses IDP_*_SECRET for the real pools; that is the
+  SQL seed path and its names are not interchangeable with this one.)"
 fi
 command -v go      >/dev/null 2>&1 || fail "go toolchain required (provision runs via 'go run')"
 command -v kubectl >/dev/null 2>&1 || fail "kubectl required"
diff --git a/scripts/kind/verify-mock-oauth.sh b/scripts/kind/verify-mock-oauth.sh
--- a/scripts/kind/verify-mock-oauth.sh
+++ b/scripts/kind/verify-mock-oauth.sh
@@ -1,33 +1,15 @@
 #!/usr/bin/env bash
 #
-# ============================================================================
-# KNOWN BROKEN since KCS-1407 — this script cannot pass as written.
-#
-# It drives the in-cluster mock IdP (mock-oauth2-server) over plaintext http at
-# a ClusterIP. KCS-1407 screens a provider's oidcConfig.discoveryUrl as an
-# outbound destination — https to a public host only, private/loopback/
-# link-local and cloud metadata refused. Two things followed: an in-cluster IdP
-# could not be registered at all, and scripts/post-deploy/envs/kind/manifest.yaml
-# was re-seeded onto real Cognito pools.
-#
-# KCS-1468 removed the FIRST blocker. kind now sets global.idpAllowPrivateTargets
-# and global.idpAllowInsecureScheme, so a plaintext in-cluster discoveryUrl is
-# registrable here again.
-#
-# The SECOND blocker stands, and it is why this is still broken: the kind
-# manifest seeds real Cognito, so no IdP resolves to this mock and the fixture
-# users (admin@kind.test, alice@acme.test, bob@acme-eng.test) do not exist in
-# those pools. /login/initiate will still not produce a mock authorize URL for
-# admin@kind.test. Restoring this flow needs a manifest change — re-seeding a
-# mock IdP row and its users — not a code change.
-#
-# Deliberately left in place rather than deleted or rewritten. It is the
-# KCS-1360/1361/1363 both-modes coverage and belongs to the kind local-env
-# tooling (KCS-840, KCS-1217), so what replaces it is a call for that tooling's
-# owner, not a drive-by change inside a security fix. Options, roughly: point it
-# at a real IdP with real credentials, or re-scope it to the callback-mode
-# assertions it uniquely covers, driven by a real login.
-# ============================================================================
+# Was KNOWN BROKEN between KCS-1407 and KCS-1468, in two stages. KCS-1407 began
+# screening a provider's oidcConfig.discoveryUrl as an outbound destination
+# (https to a public host only; private/loopback/link-local and cloud metadata
+# refused), which the mock — plaintext http at a ClusterIP — cannot satisfy, so
+# it could not be registered at all and the kind manifest was re-seeded onto
+# real Cognito pools. KCS-1468 made that screening relaxable and kind sets both
+# opt-outs (global.idpAllowPrivateTargets, global.idpAllowInsecureScheme),
+# lifting the first blocker; the seed was moved back in step with this comment,
+# lifting the second. Both halves are needed — a registrable mock nothing
+# resolves to still fails here.
 #
 # verify-mock-oauth.sh — Drive full OIDC logins as admin@kind.test through the
 # kind-only mock external IdP (k0rdent-kind-extras chart) and assert the resulting
diff --git a/scripts/post-deploy/envs/kind/manifest.yaml b/scripts/post-deploy/envs/kind/manifest.yaml
--- a/scripts/post-deploy/envs/kind/manifest.yaml
+++ b/scripts/post-deploy/envs/kind/manifest.yaml
@@ -4,24 +4,22 @@
 # (auth-service OIDC -> Keycloak JWT -> Kong) and for populating the Atlas UI.
 # Applied by the KCS-404 `provision` tool through the service HTTP APIs:
 #
+#   make kind-multi-install-extras   # deploy the mock first (IAM validates the
+#                                    # IdP discovery URLs at registration)
 #   make kind-multi-provision-mock   # apply this manifest on the GLOBAL cluster
 #
-# The identity providers are real Cognito pools (KCS-1407 — see the note above
-# the identityProviders block). Export their client secrets first, using the
-# same names every other manifest-provisioned env uses:
+# The identity providers are the in-cluster mock-oauth2-server from the
+# k0rdent-kind-extras chart (KCS-1468 — see the note above the identityProviders
+# block). Every row reads one throwaway secret, which mock-oauth2-server does not
+# validate, so any non-empty value works:
 #
-#   export VENDOR_IDP_CLIENT_SECRET=...  PROVIDER_IDP_CLIENT_SECRET=... \\
-#          CIAM_IDP_CLIENT_SECRET=...
+#   export KIND_IDP_SECRET=placeholder
 #
-# mock01, dev01 and dev02 declare these same names, and they are what
-# docs/operations/environment-bootstrap.md tells operators to export, so one
-# export set serves every manifest-provisioned env including this one.
-# provision-mock-idp.sh preflights all three and names the missing one rather
-# than failing opaquely at apply time.
-#
-# NOTE: scripts/post-deploy/seed.sh uses IDP_*_SECRET for the same pools. That
-# is the SQL seed path, a separate mechanism with its own naming — the two are
-# not interchangeable.
+# provision-mock-idp.sh preflights it and names it rather than failing opaquely
+# at apply time. Unlike mock01/dev01/dev02 this env needs no real pool secret:
+# nothing here points at Cognito, so the VENDOR_/PROVIDER_/CIAM_IDP_CLIENT_SECRET
+# export set docs/operations/environment-bootstrap.md describes is not required
+# to provision kind.
 #
 # Shape (mirrors a mock env, kept small): one operator org + one customer org.
 #
@@ -31,10 +29,12 @@
 #   acme  acme-main  alice@acme.test      compute-admin     customer-portal
 #   acme  acme-main  bob@acme-eng.test    compute-viewer    customer-portal
 #
-# The users below are fixture identities for the header-driven e2e paths. Since
-# KCS-1407 they cannot complete an interactive login: the IdPs are real Cognito
-# pools and these addresses do not exist in them. Log in with a real pool user
-# (vendor.com / testcustomer.com) instead.
+# The users below are fixture identities for the header-driven e2e paths, and
+# each can also complete a full interactive login against the mock IdP — that is
+# what `make kind-multi-verify-mock-oauth` drives. Every loginable user needs a
+# tokenCallbacks entry in the extras chart AND an identityProviders row below
+# that resolves its email domain; the two must agree on issuer path, client id
+# and email.
 #
 # User ids are required so `policies` can reference the users. IAM honours
 # caller-supplied ids but enforces ^usr-[a-z0-9]+(-[a-z0-9]+)*$ (KCS-863) on them
@@ -129,90 +129,89 @@
     displayName: Kind Admin - compute-admin (platform)
 
 identityProviders:
-# KCS-1407 — these were previously the in-cluster mock-oauth2-server on plaintext
-# http (http://mock-oauth2-server.k0rdent-apis.svc:8080/...). A customer-supplied
-# discoveryUrl is now screened as an outbound destination — https to a public
-# host, with private/loopback/link-local and cloud-metadata targets refused — so
-# an in-cluster IdP could not be registered at all, and kind moved to the same
-# real Cognito pools as mock01/dev02, with secrets from the operator's
-# environment (see docs/operations/environment-bootstrap.md and
-# scripts/post-deploy/seed.sh).
+# In-cluster mock-oauth2-server (k0rdent-kind-extras). Each row's issuer path +
+# oidcClientId must match a tokenCallbacks entry in the chart's values.yaml, and
+# the email that entry claims is what the auth-service links the user by.
 #
-# KCS-1468 has since made that screening relaxable, and kind sets both opt-outs
-# (global.idpAllowPrivateTargets, global.idpAllowInsecureScheme), so an
-# in-cluster mock IdP CAN be seeded here again. These entries were deliberately
-# left on Cognito rather than reverted: that is a kind local-env tooling
-# decision, and the fixture users below would need restoring in step with it.
+# KCS-1407 moved these rows to real Cognito pools: it screens a customer-supplied
+# discoveryUrl as an outbound destination — https to a public host, with
+# private/loopback/link-local and cloud-metadata targets refused — which an
+# in-cluster ClusterIP on plaintext http cannot satisfy by construction.
 #
-# Consequence, deliberately accepted: the mock fixture users (admin@kind.test,
-# alice@acme.test, bob@acme-eng.test) can no longer complete an interactive
-# login, because they do not exist in these pools. They remain for the
-# header-driven e2e paths.
+# KCS-1468 made that screening relaxable per deployment, and kind sets both
+# opt-outs (global.idpAllowPrivateTargets, global.idpAllowInsecureScheme in
+# values-secret-kind.yaml), so the mock is registrable here again. That left the
+# seed as the only remaining blocker to a local login, which is what this block
+# restores — see the header of scripts/kind/verify-mock-oauth.sh.
 #
-# Which provider each of them RESOLVES to changed with KCS-1749: the acme entry
-# below now claims `acme.test` (it previously claimed ncprovider.com), so
-# alice@acme.test resolves to the acme provider rather than to the CIAM
-# fallback. admin@kind.test and bob@acme-eng.test still fall back, because no
-# provider claims kind.test or acme-eng.test. None of them can log in either
-# way — the note above is unaffected — but the invitation domain guard reads
-# exactly this ownership, which is what makes alice the accept case and bob the
-# refuse case on kind.
-# `make kind-multi-verify-mock-oauth` fails for this reason — the seed, no
-# longer the guard — see the note at the top of scripts/kind/verify-mock-oauth.sh.
+# WHAT EACH ROW IS FOR. Three of the four exist to keep an invitation-guard case
+# testable on kind, so read the guard before editing a tenantDomain:
 #
-# KCS-1384 is preserved: the `vendor` org keeps a registered tenant_domain so
-# the special-org invitation guard's accept path stays testable here, and
-# `provider` still deliberately gets none — an org with no registered domain is
-# the case that proves the guard fails closed rather than falling back. Only the
-# IdP behind vendor changed, from the mock to the real vendor pool.
-  - displayName: "Vendor IdP (Cognito)"
-    org: vendor
-    tenantDomain: vendor.com
-    oidcDiscoveryUrl: https://cognito-idp.us-west-2.amazonaws.com/us-west-2_217ihZppD/.well-known/openid-configuration
-    oidcClientId: 5sq4p6kv122e22o1qba7ndmads
+#   org       domain          guard case
+#   --------  --------------  ----------------------------------------------
+#   acme      acme.test       KCS-1749 ACCEPT — alice is in-domain
+#   (none)    acme-eng.test   KCS-1749 REFUSE — no provider claims bob's domain
+#   vendor    vendor.test     KCS-1384 ACCEPT — special org with a domain
+#   provider  (no row)        KCS-1384 FAIL-CLOSED — special org without one
+  - displayName: Kind IdP (operator)
+    org: kind
+    tenantDomain: kind.test
+    oidcDiscoveryUrl: http://mock-oauth2-server.k0rdent-apis.svc:8080/kind/.well-known/openid-configuration
+    oidcClientId: kind-mock-client
     oidcClientSecret:
-      env: VENDOR_IDP_CLIENT_SECRET
+      env: KIND_IDP_SECRET
     enabled: true
     isDefault: true
-  # org `acme` is the customer-side fixture. It borrows the provider POOL because
-  # that is one of the three pools whose secret the documented export set covers
-  # (vendor / provider / ciam).
-  #
-  # tenantDomain is NOT the pool's domain and does not have to match it: the pool
-  # is where sign-in happens (oidcDiscoveryUrl), tenantDomain is the domain this
-  # org claims for routing and ownership. It was `ncprovider.com` only because
-  # the borrowed pool suggested it, and KCS-1749 made that inconsistency bite:
-  # acme's own fixture users are alice@acme.test / bob@acme-eng.test, so with
-  # acme claiming ncprovider.com every invitation to its own users would be
-  # refused and kind would have no working example of the in-domain accept path.
-  #
   # `acme.test` is deliberately alice's domain and NOT bob's (acme-eng.test),
-  # which gives kind both halves of the guard from one seed: alice is in-domain
-  # and accepted, bob is on a domain no provider claims and refused. Do not
-  # "fix" bob by registering acme-eng.test — that would delete the negative case.
-  #
-  # Neither user can complete an interactive login here regardless (see the note
-  # above: they do not exist in these real Cognito pools). The invitation guard
-  # is a create-time check that never reaches the IdP, so it is fully testable
-  # on kind anyway.
-  - displayName: "Acme IdP (Cognito, provider pool)"
+  # which gives kind both halves of the KCS-1749 guard from one seed: alice is
+  # in-domain and accepted, bob is on a domain no provider claims and refused.
+  # Do not "fix" bob by registering acme-eng.test here — that would delete the
+  # negative case. Bob logs in through the fallback row below instead.
+  - displayName: Acme IdP (alice)
     org: acme
     tenantDomain: acme.test
-    oidcDiscoveryUrl: https://cognito-idp.us-west-2.amazonaws.com/us-west-2_DM62vEZtL/.well-known/openid-configuration
-    oidcClientId: 3107ktlhcg3b3rlhimik37ubr8
+    oidcDiscoveryUrl: http://mock-oauth2-server.k0rdent-apis.svc:8080/acme/.well-known/openid-configuration
+    oidcClientId: kind-mock-acme-client
     oidcClientSecret:
-      env: PROVIDER_IDP_CLIENT_SECRET
+      env: KIND_IDP_SECRET
     enabled: true
     isDefault: true
-  # Platform-wide CIAM fallback (org_id = ''), used when an email domain matches
-  # no org-scoped IdP — which is now the case for the .test fixture users.
-  - displayName: "Default CIAM (Cognito)"
+  # KCS-1384: the `vendor` org is seeded on every environment by the orgs
+  # 000003_seed migration, but kind registered no domain for it — so the
+  # special-org invitation guard (which requires the invitee's domain to be
+  # owned by the target org) could only ever fail closed here, leaving its
+  # accept path untestable locally. dev01, dev02 and mock01-04 already
+  # register domains for both special orgs.
+  #
+  # `provider` deliberately gets none: an org with no registered domain is
+  # itself a case worth exercising, and it is the one that proves the guard
+  # fails closed rather than falling back.
+  - displayName: Vendor IdP (kind)
+    org: vendor
+    tenantDomain: vendor.test
+    oidcDiscoveryUrl: http://mock-oauth2-server.k0rdent-apis.svc:8080/vendor/.well-known/openid-configuration
+    oidcClientId: kind-mock-vendor-client
+    oidcClientSecret:
+      env: KIND_IDP_SECRET
+    enabled: true
+    isDefault: true
+  # Platform-wide fallback (org_id = ''), used when an email domain matches no
+  # org-scoped IdP. On kind that is bob@acme-eng.test, deliberately — see the
+  # acme row — so the fallback is pointed at the mock's `acme-eng` issuer and it
+  # is bob who comes back from it. tenantDomain stays `ciam.internal` rather
+  # than acme-eng.test: claiming bob's domain from here would give the KCS-1749
+  # guard an owner to find, and the refuse case turns on there being none.
+  #
+  # Being the fallback, this row also answers for any OTHER unmatched domain,
+  # and would hand back bob's identity for it. That is a mock talking, not a
+  # pool: one issuer serves one identity, and on kind nothing else falls through.
+  - displayName: Kind fallback IdP (bob, eng)
     org: ""
     tenantDomain: ciam.internal
-    oidcDiscoveryUrl: https://cognito-idp.us-west-2.amazonaws.com/us-west-2_yu7xGZIRa/.well-known/openid-configuration
-    oidcClientId: 1d1o91en9p0dti6kec9repuc18
+    oidcDiscoveryUrl: http://mock-oauth2-server.k0rdent-apis.svc:8080/acme-eng/.well-known/openid-configuration
+    oidcClientId: kind-mock-acme-eng-client
     oidcClientSecret:
-      env: CIAM_IDP_CLIENT_SECRET
+      env: KIND_IDP_SECRET
     enabled: true
     isDefault: true
 
