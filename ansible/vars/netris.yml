sdn_provider: netris
ufo_fabric_backend: 'netris'

# Netris controller version. Switch/softgate agent packages must match.
netris_ctl_version: "4.16.0"

# Controller HA chart (OCI). Release name must stay netris-controller-ha so
# netris-controller.fullname agrees with the bundled subchart service names.
netris_controller_chart_repo: oci://registry.ci.mirantis.com/pvt-vsaienko/charts/netris/netris-controller-ha
netris_controller_chart_version: 0.13.0-alpha.0

# mariadb-operator reconciles the MariaDB/MaxScale/SqlJob CRs the controller
# chart creates. Operator chart is OCI; CRDs still come from the upstream repo
# (mariadb-operator-crds is a separate chart, not republished under charts/netris).
mariadb_operator_chart_repo: oci://registry.ci.mirantis.com/pvt-vsaienko/charts/netris/mariadb-operator
mariadb_operator_chart_version: 25.10.4
mariadb_operator_crds_chart_repo: https://mariadb-operator.github.io/mariadb-operator

# Traefik fronts the controller and terminates the fabric-facing ports
# (grpc 50051, telescope 3033/3034, graphite 2003) that haproxy used to serve.
# Must stay on 27.x/Traefik v2 - v3 drops the traefik.containo.us CRD group that
# the controller chart's routers are written against.
traefik_chart_repo: https://traefik.github.io/charts
traefik_chart_version: 27.0.2
traefik_namespace: traefik

# Inventory profiles (InventoryProfile CRs in platform-admin)
inventory_profile_name: default-inventory-profile
ew_inventory_profile_name: roce-profile

br_mgmt_phys_interface: ens5
br_ext_phys_interface: "ens4"
