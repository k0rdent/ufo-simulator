---
- name: Build nico-core-mock Docker image
  ansible.builtin.command:
    cmd: >-
      docker build
      -f nico-core-mock/Dockerfile
      -t {{ nico_core_mock_image }}
      .
    chdir: "{{ nico_rest_workspace }}"
